<h1>Edit a Meal</h1>
<p>Find me in app/views/meals/edit.html.erb</p>

<%= form_with(model: @meal, local: true) do |f| %>
  <div class="field">
  <%= f.label :name %>
  <%= f.text_field :name, required: true, placeholder: "Name" %>
  <%= f.submit 'updated list' %>
  </div>

  <% end %>
